export default function JwtComponent() {
  return (
    <div>
      <h2>JWT</h2>
      <p>
        JWT (JSON Web Token) is a compact, URL-safe means of representing
        claims.
      </p>
    </div>
  );
}
